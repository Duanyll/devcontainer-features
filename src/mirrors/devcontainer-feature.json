{
    "name": "Mirrored Repositories",
    "id": "mirrors",
    "version": "1.1.0",
    "description": "Use mirrored repositories to speed up package installation",
    "options": {
        "increaseAptRetry": {
            "type": "boolean",
            "default": true,
            "description": "Increase the number of retries for apt-get for poor network connections"
        },
        "debian": {
            "type": "string",
            "default": "",
            "description": "Debian mirror URL"
        },
        "debianSecurity": {
            "type": "string",
            "default": "",
            "description": "Debian security mirror URL (It is not recommended to use a mirror for security updates)"
        },
        "ubuntu": {
            "type": "string",
            "default": "",
            "description": "Ubuntu mirror URL"
        },
        "ubuntuSecurity": {
            "type": "string",
            "default": "",
            "description": "Ubuntu security mirror URL (It is not recommended to use a mirror for security updates)"
        },
        "alpine": {
            "type": "string",
            "default": "https://dl-cdn.alpinelinux.org/alpine",
            "description": "Alpine mirror URL"
        },
        "pip": {
            "type": "string",
            "default": "",
            "description": "Python pip mirror URL"
        }
    }
}